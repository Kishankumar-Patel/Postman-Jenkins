{
  "name": "postman",
  "version": "1.0.0",
  "description": "Project for Storing Postman tests",
  "scripts": {
    "templateService-test": "newman run tests/TemplateService-Automate.postman_collection.json -d data/data-templateService.csv -e environment/dev-91.json --reporters cli,junit,html --reporter-junit-export newman/templateService.xml --reporter-junit-export newman/templateService.html"
  },
  "author": "Kishank Patel",
  "dependencies": {
    "newman": "^5.0.0"
  }
}
